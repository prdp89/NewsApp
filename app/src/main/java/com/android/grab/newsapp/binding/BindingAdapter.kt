package com.android.grab.newsapp.binding

/*@BindingAdapter("imageUrl")
fun bindImageUrl(imageView: ImageView, url: String?) {
    if (null != url) {

        val glideUrl = GlideUrl(url)
        Glide.with(imageView.context)
            .load(glideUrl)
            .apply(RequestOptions().placeholder(R.drawable.place_holder).error(R.drawable.place_holder).circleCrop())
            .diskCacheStrategy(DiskCacheStrategy.ALL)
            .into(imageView)
    } else {
        imageView.setImageResource(R.drawable.place_holder)
    }
}*/

/*
@BindingAdapter("detailImageUrl")
fun bindDetailImageUrl(imageView: ImageView, url: String?) {
    if (null != url) {

        val glideUrl = GlideUrl(url)
        Glide.with(imageView.context)
            .load(glideUrl)
            .apply(RequestOptions().placeholder(R.drawable.place_holder).error(R.drawable.place_holder))
            .diskCacheStrategy(DiskCacheStrategy.ALL)
            .into(imageView)
    } else {
        imageView.setImageResource(R.drawable.place_holder)
    }
}*/
